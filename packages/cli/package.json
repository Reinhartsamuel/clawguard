{
  "name": "@reinhartsamuel/clawguard",
  "version": "0.1.1",
  "description": "Transparent API proxy and firewall for AI spending — budget caps, anomaly detection, loop prevention",
  "license": "MIT",
  "homepage": "https://clawguard.dev",
  "repository": {
    "type": "git",
    "url": "https://github.com/Reinhartsamuel/clawguard"
  },
  "type": "module",
  "bin": {
    "clawguard": "./dist/index.js"
  },
  "files": [
    "dist/",
    "README.md"
  ],
  "scripts": {
    "dev": "bun src/index.ts",
    "build": "tsup && bun -e \"const {cpSync}=require('fs');cpSync('../proxy/dist','./dist/proxy-runtime',{recursive:true});cpSync('../../README.md','./README.md');\""
  },
  "dependencies": {
    "@clawguard/proxy": "workspace:*"
  },
  "peerDependencies": {
    "bun": ">=1.0"
  },
  "devDependencies": {
    "tsup": "^8.4.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0",
    "@types/bun": "^1.2.0"
  }
}
